[build]
  command = "npm run build"
  publish = "build"

# Serve the static privacy policy before the SPA catch-all so it loads without JS.
[[redirects]]
  from = "/privacy"
  to = "/privacy.html"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
